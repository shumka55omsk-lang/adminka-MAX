# V52 — MAX opening coefficient sync

Исправление расхождения суммы между уведомлением MAX и CRM мягких окон.

## Что изменено

- Серверный расчёт mini app теперь учитывает коэффициент открывающихся окон `×1.6`, как старая CRM при включённой галочке «Открывающиеся».
- В MAX-уведомление уходит сумма уже с коэффициентом.
- В `estimated_total` таблицы `max_miniapp_leads` сохраняется итог с коэффициентом.
- В `raw_payload.estimate` добавлены поля:
  - `subtotalBeforeOpening`
  - `openingCoefficient`
  - `openingMarkup`
  - `subtotal`
- Суммы в `items[]` также учитывают коэффициент, чтобы JSON расчёта не расходился с `totals.grandTotal`.

## Проверка

Пример 500×500 см, с монтажом, открывающиеся:

- площадь 25 м²
- изделие: 25 × 1800 = 45 000 ₽
- монтаж: 25 × 700 = 17 500 ₽
- до коэффициента: 62 500 ₽
- открывающиеся ×1.6 = 100 000 ₽

Теперь MAX и CRM должны показывать 100 000 ₽.
